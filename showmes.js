import * as text from "./greetings";
var name='Маша';
function showMessage(){
  alert (${greeting.first}+", "+ name);
  var age = prompt("Сколько тебе леt?");
    if (100> age >0) {
      alert("Поздравляю)")
    }

        else {alert ("Манишь)")
  };
  alert(${greeting.last} +", "+ name);

}
showMessage();
